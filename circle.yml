machine:
  php:
    version: 7.0.7

dependencies:
  cache_directories:
    - "~/.composer/cache"
  override:
    - composer install --no-interaction

test:
  override:
    - vendor/bin/phpcs --colors --extensions=php --standard=PSR2 src/ bin/
    - vendor/bin/phpunit tests/
